Pasos a seguir para ejecutar el programa desde la terminal:

- Iniciar la terminal en la carpeta raíz del proyecto

- Escribir:
    javac -d bin src/main/java/level_1/exercici_1/Main.java
    Esto compilará el programa en una carpeta llamada bin en la raíz del proyecto

- Para ejecutar el programa, escribir en terminal:
    java -cp bin main/java/level_1/exercici_1/Main

- Devuelve por terminal:
    - doc1
    - doc2
    - password

El sistema operativo utilizado es Linux.
Otro sistema operativo puede usar otro tipo de forma de escribir rutas que no use /